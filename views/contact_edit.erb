please edit the contact and click 'save changes'

<form action="/contacts/<%= contact.id %>" method="PUT">
  name: <input value=<%= contact.name %> type="text" name="name">
  <input type="submit" value='save changes'>
</form>